import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "ATS Resume Checker - Optimize Your Resume for Success",
  description:
    "Upload your resume and get instant ATS compatibility analysis with detailed feedback and suggestions for improvement.",
  keywords: "ATS, resume checker, applicant tracking system, resume optimization, job search",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
